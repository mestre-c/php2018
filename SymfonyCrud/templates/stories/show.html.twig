{% extends 'base.html.twig' %}

{% block title %} {{ story.title }} {% endblock %}

{% block body %}
   <h1>{{ story.title }}</h1>
    <p>{{ story.body }}</p>
    <hr>
    <a href="/">Go Back</a>
{% endblock %}
